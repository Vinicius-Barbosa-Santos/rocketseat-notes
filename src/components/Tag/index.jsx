// import Styles
import * as C from './styles'

// eslint-disable-next-line react/prop-types
export const Tag = ({ title }) => {
    return (
        <C.Container>
            {title}
        </C.Container>
    )
}